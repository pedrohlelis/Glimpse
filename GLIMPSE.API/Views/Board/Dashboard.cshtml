@using GLIMPSE.DOMAIN.ViewModels
@model BoardVM;
<div class="d-flex flex-column w-100 flex-grow">
    <div class="d-flex w-100 flex-grow">
        <div class="d-flex flex-grow">
            <img src="@Model.Board.Project.Picture" alt="" width="60px" height="60px">
            <h1 class="">@Model?.Board?.Project?.Name</h1>
        </div>
        <div>
            <button><img src="" alt=""><span>Convidar</span></button>
        </div>
    </div>
    <div class="d-flex">
        oba
        <div></div>
        <div></div>
    </div>
</div>
